const sum = (x, y) => {
    return x + y;
}

const subtract = (x, y) => {
    return x - y;
}

const division = (x, y) => {
    return x / y;
}

const multiplication = (x, y) => {
    return x * y;
}

module.exports = { sum , subtract, division, multiplication };


